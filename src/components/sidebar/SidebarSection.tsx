/**
 * Thin re-export of CollapsibleSection under the name used by the v1.1
 * sidebar structure. Keeping the underlying component identical preserves
 * existing behavior; callers in new code should import this module.
 */
export { CollapsibleSection as SidebarSection } from './CollapsibleSection';
