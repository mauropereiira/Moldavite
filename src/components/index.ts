/**
 * React components for Notomattic
 *
 * ## Component Groups
 *
 * - `layout` - App layout (Layout, Header)
 * - `sidebar` - Sidebar navigation (Sidebar, NotesList)
 * - `editor` - Note editor (NoteEditor, Toolbar)
 * - `calendar` - Calendar views and integration
 * - `toast` - Toast notification container
 * - `updates` - App update notifications
 *
 * ## Other Components (not re-exported)
 *
 * - `settings/` - Settings modal and sections
 * - `modals/` - Various modal dialogs
 * - `templates/` - Template management UI
 *
 * @module components
 */

export * from './layout';
export * from './sidebar';
export * from './editor';
export * from './calendar';
export * from './toast';
export * from './updates';
