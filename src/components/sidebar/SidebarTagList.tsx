/**
 * The "Tags" sidebar section. Currently a thin wrapper re-export over
 * the existing TagsSection component to honor the v1.1 naming scheme.
 * Future tag-specific work should happen here or inside TagsSection.
 */
export { TagsSection as SidebarTagList } from './TagsSection';
